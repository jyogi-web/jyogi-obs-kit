## OBS配信の実行計画

### 初回セットアップ

1. OBSをインストールして起動する。
2. `jyogi-obs-kit` リポジトリを取得する。
3. OBSへ `profiles` 内のプロファイルをインポートする。
4. OBSへ `scenes/jyogi-obs-kit.json` のシーンコレクションをインポートする。
5. `jyogi-obs-kit` のプロファイルとシーンコレクションを選択する。

### 配信前の準備

1. Discordを起動し、配信するボイスチャンネルへ接続する。
2. `けろこ会` または `部会` のどちらで配信するか選択する。
3. 選択した配信シーンの `Discord配信画面` で、現在のDiscord本体ウィンドウを選択する。
4. 共通の `待機画面` シーンを表示する。
5. YouTubeで配信枠を準備する。

### 配信開始

1. OBSで `配信開始` を押す。
2. 配信が開始されたことを確認する。
3. `待機画面` から、選択した `けろこ会配信` または `部会配信` シーンへ切り替える。
4. 映像、Discord音声、会場マイクの音声を確認する。

### 配信中

- 発表者を切り替えるときは、Discord内で表示する画面共有を切り替える。
- OBS側のシーンやウィンドウキャプチャを毎回変更する必要はない。
- 音声メーターと映像が正常に動作していることを確認する。

### 配信終了

1. 必要に応じて `待機画面` シーンへ戻す。
2. OBSで `配信終了` を押す。
3. YouTube側で配信が終了したことを確認する。
4. Discordから退出する。
